require 'nvim-treesitter.configs'.setup {
  esure_installed = {'nodejs', 'python', 'vim', 'svelte', 'javascript', 'html'},

  sync_install = false,
  auto_install = true,
  highlight = {
    enabled = true,
  },
}
